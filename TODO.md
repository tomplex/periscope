# periscope — todo

- Add tools for tmux session / window management (create, rename, kill, etc.).
- Help manage git worktrees when appropriate — create the worktree and start a Claude in it.
- Add a built-in TODO list for the session to capture ideas
  - along with maybe a built-in claude to dispatch those tasks?
- sometimes modal tabs become pinned so that you cannot reach the bottom
- remove focus in tmux button
- claude working / idle detection STILL not working
